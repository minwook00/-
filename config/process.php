<?php

return [

    /*
    |--------------------------------------------------------------------------
    | PHP Binary Path
    |--------------------------------------------------------------------------
    |
    | PHP CLI 바이너리의 경로를 설정합니다. 기본값은 'php'로 시스템 PATH에서
    | 탐색합니다. 카페24 등 비표준 경로에 PHP가 설치된 호스팅 환경에서는
    | 절대 경로를 지정합니다 (예: /usr/local/php82/bin/php).
    |
    */

    'php_binary' => env('PHP_BINARY', 'php'),

    /*
    |--------------------------------------------------------------------------
    | Composer Binary Path
    |--------------------------------------------------------------------------
    |
    | Composer 바이너리의 경로를 설정합니다. 비어있으면 시스템 PATH에서
    | 'composer'를 탐색합니다. .phar 파일 경로를 지정하면 PHP 바이너리를
    | 사용하여 실행합니다 (예: /usr/local/bin/composer.phar).
    |
    */

    'composer_binary' => env('COMPOSER_BINARY', ''),

];
