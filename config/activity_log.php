<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 활동 로그 활성화
    |--------------------------------------------------------------------------
    |
    | 활동 로그 기록을 활성화/비활성화합니다.
    | false로 설정하면 ActivityLogHandler에서 DB 저장을 건너뜁니다.
    |
    */
    'enabled' => env('ACTIVITY_LOG_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | 활동 로그 채널
    |--------------------------------------------------------------------------
    |
    | config/logging.php에 정의된 Monolog 채널명입니다.
    | 기본값 'activity'는 ActivityLogChannel을 통해 DB에 저장됩니다.
    |
    */
    'channel' => env('ACTIVITY_LOG_CHANNEL', 'activity'),
];
